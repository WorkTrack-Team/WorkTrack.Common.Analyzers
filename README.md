# WorkTrack.Common.Analyzers

Общие Roslyn анализаторы и настройки для всех сервисов WorkTrack.

**Версия**: 1.0.0  
**Статус**: ✅ В разработке

## Описание

Пакет предоставляет единообразные Roslyn анализаторы и конфигурацию через `buildTransitive/WorkTrack.Common.Analyzers.props` для всех сервисов WorkTrack, гарантируя идентичные правила качества кода во всех проектах.

## 📋 Что входит в пакет

При установке пакета автоматически применяются:

1. **Roslyn Analyzers** (WTI0001-WTI0007) - проверка кода во время компиляции
2. **Настройки через `buildTransitive`** - автоматическое подключение стандартных анализаторов
3. **Конфигурация через `.editorconfig`** - настройка параметров анализаторов

**Файл `buildTransitive/WorkTrack.Common.Analyzers.props` импортируется только в проекты с прямой ссылкой на пакет.** Это означает, что нужно добавить `PackageReference` в **каждый `.csproj`**, который должен использовать анализаторы.

Рекомендуемый подход:
1. Добавить версию в `Directory.Packages.props` (централизованное управление)
2. Добавить `PackageReference Include="WorkTrack.Common.Analyzers"` в каждый `.csproj`
3. Можно автоматизировать через скрипт (см. [INSTALLATION.md](INSTALLATION.md))

## Анализаторы

- **WTI0001** - `MethodLengthAnalyzer` - Метод не должен содержать более N строк (настраивается через .editorconfig)
- **WTI0002** - `AsyncSuffixAnalyzer` - Асинхронные методы должны оканчиваться на `Async`
- **WTI0004** - `ForbiddenDateTimeAnalyzer` - Запрет прямого использования `DateTime`/`DateTimeOffset`
- **WTI0005** - `GuardClauseAnalyzer` - Требование `Guard.Against` в начале публичных методов
- **WTI0006** - `NamespaceStyleAnalyzer` - Требование file-scoped namespace
- **WTI0007** - `NamespaceStyleAnalyzer` - Только один публичный тип в файле

## Конфигурация через .editorconfig

### Настройка максимального количества строк в методе (WTI0001)

```ini
[*.cs]
# По умолчанию: 5 строк
wt_common_method_length_max_lines = 5

# Для конкретного проекта можно изменить:
[WorkTrack.SomeService/**/*.cs]
wt_common_method_length_max_lines = 10

# Для тестов можно отключить или увеличить лимит:
[tests/**/*.cs]
wt_common_method_length_max_lines = 100
dotnet_diagnostic.WTI0001.severity = none
```

### Настройка уровней серьезности

```ini
[*.cs]
# Все анализаторы как ошибки (по умолчанию)
dotnet_diagnostic.WTI0001.severity = error
dotnet_diagnostic.WTI0002.severity = error
dotnet_diagnostic.WTI0004.severity = error
dotnet_diagnostic.WTI0005.severity = error
dotnet_diagnostic.WTI0006.severity = error
dotnet_diagnostic.WTI0007.severity = error

# Для тестов можно отключить некоторые правила
[tests/**/*.cs]
dotnet_diagnostic.WTI0001.severity = none
```

## 🚀 Быстрый старт

### Установка пакета

1. **Добавить версию пакета в `Directory.Packages.props`** (если используется централизованное управление):
```xml
<ItemGroup>
  <PackageVersion Include="WorkTrack.Common.Analyzers" Version="1.0.0" />
</ItemGroup>
```

2. **Добавить ссылку на пакет в каждый `.csproj`**, который должен использовать анализаторы:
```xml
<ItemGroup>
  <PackageReference Include="WorkTrack.Common.Analyzers" />
</ItemGroup>
```

**Важно:** Пакет нужно добавить в **каждый проект**, который должен анализироваться. Анализаторы не применяются автоматически ко всему solution.

### Автоматическая установка

Используйте скрипт для добавления пакета во все проекты solution:

**PowerShell:**
```powershell
$projects = Get-ChildItem -Recurse -Filter "*.csproj" | Where-Object { $_.FullName -notmatch "Tests|WorkTrack.Common.Analyzers" }
foreach ($project in $projects) {
    dotnet add $project.FullName package WorkTrack.Common.Analyzers
}
```

**Bash:**
```bash
find . -name "*.csproj" -not -path "*/Tests/*" -not -path "*/WorkTrack.Common.Analyzers/*" | while read proj; do
    dotnet add "$proj" package WorkTrack.Common.Analyzers
done
```

### Настройка через .editorconfig

После установки пакета настройки применяются автоматически через `buildTransitive`. Для кастомизации создайте или обновите `.editorconfig` в корне проекта:

```ini
root = true

[*.cs]
# Максимальное количество строк в методе (по умолчанию: 5)
wt_common_method_length_max_lines = 5

# Префиксы проектов для анализа (по умолчанию: WorkTrack.*)
wt_common_analyzer_target_prefixes = WorkTrack.Identity, WorkTrack.TemplateService

# Уровни серьезности анализаторов
dotnet_diagnostic.WTI0001.severity = error  # Метод слишком длинный
dotnet_diagnostic.WTI0002.severity = error  # Отсутствует суффикс Async
dotnet_diagnostic.WTI0004.severity = error  # Использование DateTime.Now
dotnet_diagnostic.WTI0005.severity = error  # Отсутствует Guard.Against
dotnet_diagnostic.WTI0006.severity = error  # Неправильный стиль namespace
dotnet_diagnostic.WTI0007.severity = error  # Неправильный стиль namespace
```

### Проверка установки

После установки пересоберите проект:
```bash
dotnet build
```

Анализаторы должны начать работать автоматически. Если видите предупреждения/ошибки от `WTI0001-WTI0007`, значит установка прошла успешно.

## 📖 Дополнительная информация

> 📖 **Подробные инструкции по установке**: [INSTALLATION.md](INSTALLATION.md)  
> 📖 **Использование и настройка**: [USAGE.md](USAGE.md)  
> 📖 **Экспертное ревью**: [EXPERT_CODE_REVIEW.md](EXPERT_CODE_REVIEW.md)

## Автоматическое применение настроек через NuGet

При установке пакета `WorkTrack.Common.Analyzers` автоматически применяются настройки сборки через файл `buildTransitive/WorkTrack.Common.Analyzers.props`. Это означает, что вам **не нужно** вручную копировать или импортировать `Directory.Build.props` - все настройки применяются автоматически.

### Порядок применения настроек MSBuild

MSBuild импортирует файлы в следующем порядке (от раннего к позднему):

1. **`buildTransitive/*.props` из NuGet пакетов** - настройки из `WorkTrack.Common.Analyzers.props` (этот файл)
2. **`Directory.Build.props` из иерархии директорий** - снизу вверх по структуре папок
3. **Проект `.csproj`** - финальные настройки (имеют наивысший приоритет)

**Настройки из более поздних источников ПЕРЕОПРЕДЕЛЯЮТ настройки из более ранних.**

### Переопределение настроек в проекте

Если нужно переопределить настройки из пакета, создайте локальный `Directory.Build.props` в корне вашего проекта или сервиса:

```xml
<Project>
  <!-- Переопределяем TreatWarningsAsErrors для Debug конфигурации -->
  <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
  </PropertyGroup>

  <!-- Изменяем TargetFramework -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <!-- Добавляем дополнительные NoWarn для тестов -->
  <PropertyGroup Condition="$([System.String]::Copy('$(MSBuildProjectName)').EndsWith('Tests'))">
    <NoWarn>$(NoWarn);CA2007;MA0004</NoWarn>
  </PropertyGroup>
</Project>
```

Все свойства из `WorkTrack.Common.Analyzers.props` используют `Condition="'$(PropertyName)' == ''"`, что позволяет безопасно переопределять их в локальном `Directory.Build.props` без конфликтов.

## Преимущества

- ✅ **Автоматическое применение** - настройки применяются при установке пакета через `buildTransitive`
- ✅ **Единообразие** - одинаковые правила во всех сервисах
- ✅ **Централизованное управление** - изменения в одном месте
- ✅ **Гибкая настройка** - через .editorconfig и локальный `Directory.Build.props` для каждого проекта
- ✅ **Версионирование** - через NuGet пакет
- ✅ **Идентичная конфигурация** - гарантия одинаковых настроек
- ✅ **Безопасное переопределение** - можно переопределить любую настройку в локальном `Directory.Build.props`

## Применение в сервисах

Все сервисы WorkTrack используют этот пакет:
- `WorkTrack.Identity` - через локальный проект (будет мигрирован на NuGet)
- `WorkTrack.TemplateService` - через локальный проект (будет мигрирован на NuGet)
- Будущие сервисы - через NuGet пакет `WorkTrack.Common.Analyzers`
